[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "luminafix-style-transfer"
version = "0.2.0"
description = "LuminaFix Style Transfer"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["src*", "web*"]
